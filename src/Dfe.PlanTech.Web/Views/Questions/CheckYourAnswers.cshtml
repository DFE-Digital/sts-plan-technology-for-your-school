@using Dfe.PlanTech.Domain.Content.Models.Buttons;

@{
    var buttonWithLinkModel = new ButtonWithLink(){
        Href = "/self-assessment",
        Button = new Button(){
            IsStartButton = false,
            Value = "Save and Submit"
        }
    };

    await Html.RenderPartialAsync("Components/ButtonWithLink", buttonWithLinkModel);
}