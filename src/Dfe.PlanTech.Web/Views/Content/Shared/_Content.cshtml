@using Dfe.PlanTech.Domain.Content.Models.ContentSupport.Mapped
@model Dfe.PlanTech.Domain.Content.Models.ContentSupport.Mapped.CsContentItem;

@{
    if (Model.GetType() == typeof(RichTextContentItem))
    {
        var richText = Model as RichTextContentItem;
        <partial name="Shared/RichText/_RichText" model="@richText"/>
    }
}
