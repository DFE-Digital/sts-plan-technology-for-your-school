@model RichTextContentItem

<ol>
    @foreach (var item in Model.Content) 

        {
                if(item.NodeType == RichTextNodeType.ListItem)
                {
                    <li>
                        <partial name="RichText/_Items" model="@item"/>
                    </li>
                }
        }
</ol>