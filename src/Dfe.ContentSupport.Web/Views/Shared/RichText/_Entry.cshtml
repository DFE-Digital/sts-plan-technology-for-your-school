@model EmbeddedEntry

@{
    if (Model.JumpIdentifier is not null)
    {
        <div id=@Model.JumpIdentifier></div>
    }

    else if (Model.CustomComponent is not null)
    {
        <partial name="RichText/_GDSComponent" model="@Model.CustomComponent"/>
    }

    if (Model.RichText is not null)
    {
        <partial name="RichText/_RichText" model="@Model.RichText"/>
    }
}