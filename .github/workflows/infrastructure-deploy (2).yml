name: Infrastructure Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        description: 'The environment to run infrastructure deployment on:'
        required: true
        options: ['Dev', 'Tst', 'ProductionUnprotected', 'Production']
      is_dr:
        type: boolean
        default: false
      deploy-image:
        type: boolean
        default: true
      apply-terraform:
        type: boolean
        default: true
      restore-db:
        type: boolean
        default: false
      restore-tokens:
        type: boolean
        default: false
  push:
    branches:
      - disaster-recovery-e2e-terraform

env:
  #note you can't pass this var into the job environments, they don't calculate in time  
  DEPLOY_ENVIRONMENT: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'Dev' }}  
  ARM_CLIENT_ID: ${{ secrets.az_client_id }}
  ARM_TENANT_ID: ${{ secrets.az_tenant_id }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.az_subscription_id }}
  ARM_USE_OIDC: true
  TF_BACKEND_CONTAINER_NAME: tfstate
  TF_BACKEND_KEY: terraform.tfstate
  BACKUP_CONTAINER_NAME: sqlbackups
  TF_WORKING_DIRECTORY_ACR: terraform/acr
  TF_WORKING_DIRECTORY_APP: terraform/container-app
  TF_WORKING_DIRECTORY_DNS: terraform/dns-zone
  PACKAGE_NAME: plan-technology-for-your-school
  SQL_BACKUP_FILE_NAME: ''
  SQL_BACKUP_FILE_NAME_SEARCH: ''
  USE_LATEST_IF_NOT_SPECIFIED: true
  TF_VAR_baseurl: 'plan-technology-for-your-school.education.gov.uk'
  TF_VAR_subdomains: 'staging'
  #do we want the container-app folder to make our DNS in the same tfstate (true), or have it run a separate tf process (prod)
  TF_VAR_dns_in_app: ${{ (github.event_name == 'push') || (github.event_name == 'workflow_dispatch' && !contains(inputs.environment, 'Prod')) }}

jobs:
  image-and-acr:
    name: Apply ACR and promote image
    if: ${{ always() && !failure() && !cancelled() && (github.event_name == 'push' || inputs.deploy-image) }}
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'Dev' }}
    permissions:
      id-token: write
      contents: read
    outputs:
      image_tag: ${{ steps.promote_image.outputs.image_tag }}

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - name: Prepare derived variables
        id: vars
        shell: bash
        run: |
          set -euo pipefail

          ENVIRONMENT_NAME="${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'Dev' }}"
          IS_DR="${{ github.event_name == 'push' && true || inputs.is_dr }}"

          if [ "$IS_DR" = "true" ]; then
            DFE_PROJECT_NAME="pt-dr"
          else
            DFE_PROJECT_NAME="${{ secrets.DFE_PROJECT_NAME }}"
          fi

          RESOURCE_PREFIX_TF="${{ secrets.AZ_ENVIRONMENT }}${DFE_PROJECT_NAME}-tf"
          TF_KEYVAULT_NAME="${RESOURCE_PREFIX_TF}-kv"
          RESOURCE_PREFIX_APP="${{ secrets.AZ_ENVIRONMENT }}${DFE_PROJECT_NAME}"

          echo "ENVIRONMENT_NAME=$ENVIRONMENT_NAME" >> "$GITHUB_OUTPUT"
          echo "RESOURCE_PREFIX_TF=$RESOURCE_PREFIX_TF" >> "$GITHUB_OUTPUT"
          echo "RESOURCE_PREFIX_TF_SHORT=${RESOURCE_PREFIX_TF//-/}" >> "$GITHUB_OUTPUT"

          echo "RESOURCE_PREFIX_APP_SHORT=${RESOURCE_PREFIX_APP//-/}" >> "$GITHUB_OUTPUT"
          echo "PROJECT_PREFIX_APP=$DFE_PROJECT_NAME" >> "$GITHUB_OUTPUT"
          echo "TF_KEYVAULT_NAME=$TF_KEYVAULT_NAME" >> "$GITHUB_OUTPUT"
          echo "IS_DR=$IS_DR" >> "$GITHUB_OUTPUT"

      - name: Get runner public IP
        id: whats-my-ip
        run: |
          IP=$(curl -s https://api.ipify.org)
          echo "ip=$IP" >> "$GITHUB_OUTPUT"
          echo "TF_VAR_github_ip=$IP" >> "$GITHUB_ENV"

      - name: Login with AZ
        uses: ./.github/actions/azure-login
        with:
          az_tenant_id: ${{ secrets.az_tenant_id }}
          az_subscription_id: ${{ secrets.az_subscription_id }}
          az_client_id: ${{ secrets.az_client_id }}

      - name: Whitelist Runner for TF keyvault
        uses: ./.github/actions/whitelist-kv
        with:
          ip-address: ${{ steps.whats-my-ip.outputs.ip }}
          resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}
          keyvault-name: ${{ steps.vars.outputs.TF_KEYVAULT_NAME }}
          verb: add

      - name: Whitelist Runner for TF storage
        uses: ./.github/actions/whitelist-storage
        with:
          ip-address: ${{ steps.whats-my-ip.outputs.ip }}
          resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}
          account-name: ${{ steps.vars.outputs.RESOURCE_PREFIX_TF_SHORT }}
          verb: add

      - name: Get secret and clean tfvars file
        shell: pwsh
        run: |
          ./.github/scripts/get-and-normalise-tfvars.ps1 `
            -SecretName "${{ secrets.TF_SECRET_NAME }}" `
            -KeyVaultName "${{ steps.vars.outputs.TF_KEYVAULT_NAME }}"

      - name: Test Vars
        shell: bash
        run: |
          echo "Using tfprefix"=${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}
          echo "Using tf storage"=${{ steps.vars.outputs.RESOURCE_PREFIX_TF_SHORT }}
          echo "Using project_name=${{ steps.vars.outputs.PROJECT_PREFIX_APP }}"
          echo "Using is_dr=${{ steps.vars.outputs.IS_DR }}"

      - name: Terraform init
        working-directory: ${{ env.TF_WORKING_DIRECTORY_APP }}
        run: |
          terraform init -upgrade -reconfigure \
            -backend-config="resource_group_name=${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}" \
            -backend-config="storage_account_name=${{ steps.vars.outputs.RESOURCE_PREFIX_TF_SHORT }}" \
            -backend-config="container_name=${{ env.TF_BACKEND_CONTAINER_NAME }}" \
            -backend-config="key=${{ env.TF_BACKEND_KEY }}"

      - name: Apply Terraform changes - RG & container registry only
        working-directory: ${{ env.TF_WORKING_DIRECTORY_APP }}
        run: |
          terraform plan -input=false \
            -target=azurerm_resource_group.app_rg \
            -target=azurerm_container_registry.acr_notshared \
            -var-file="../../normalisedTerraformVars.tfvars" \
            -var="is_dr=${{ steps.vars.outputs.IS_DR }}" \
            -var="project_name=${{ steps.vars.outputs.PROJECT_PREFIX_APP }}" \
            -var="azure_location=northeurope" \
            -out=tfplan

          terraform show -no-color tfplan | grep -A20 "azurerm_resource_group.app_rg" || true

          terraform apply -auto-approve -input=false tfplan

      - name: Promote Image
        id: promote_image
        uses: ./.github/actions/promote-image
        with:
          image_branch: ${{ steps.vars.outputs.ENVIRONMENT_NAME == 'ProductionUnprotected' && 'main' || 'development' }}
          acr_name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP_SHORT }}
          acr_login_server: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP_SHORT }}.azurecr.io
          package_name: ${{ env.PACKAGE_NAME }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

  apply-terraform:
    name: Apply Terraform changes
    needs: [image-and-acr]
    if: ${{ always() && !failure() && !cancelled() && (github.event_name == 'push' || inputs.apply-terraform) }}
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'Dev' }}
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - name: Prepare derived variables
        id: vars
        shell: bash
        run: |
          set -euo pipefail

          IS_DR="${{ github.event_name == 'push' && true || inputs.is_dr }}"

          if [ "$IS_DR" = "true" ]; then
            DFE_PROJECT_NAME="pt-dr"
          else
            DFE_PROJECT_NAME="${{ secrets.DFE_PROJECT_NAME }}"
          fi

          RESOURCE_PREFIX_TF="${{ secrets.AZ_ENVIRONMENT }}${DFE_PROJECT_NAME}-tf"
          RESOURCE_PREFIX_APP="${{ secrets.AZ_ENVIRONMENT }}${DFE_PROJECT_NAME}"
          TF_KEYVAULT_NAME="${RESOURCE_PREFIX_TF}-kv"
          KEYVAULT_NAME="${RESOURCE_PREFIX_APP}-kv"

          echo "PROJECT_PREFIX_APP=$DFE_PROJECT_NAME" >> "$GITHUB_OUTPUT"
          echo "RESOURCE_PREFIX_TF=$RESOURCE_PREFIX_TF" >> "$GITHUB_OUTPUT"
          echo "RESOURCE_PREFIX_TF_SHORT=${RESOURCE_PREFIX_TF//-/}" >> "$GITHUB_OUTPUT"
          echo "RESOURCE_PREFIX_APP_SHORT=${RESOURCE_PREFIX_APP//-/}" >> "$GITHUB_OUTPUT"
          echo "TF_KEYVAULT_NAME=$TF_KEYVAULT_NAME" >> "$GITHUB_OUTPUT"
          echo "KEYVAULT_NAME=$KEYVAULT_NAME" >> "$GITHUB_OUTPUT"
          echo "IS_DR=$IS_DR" >> "$GITHUB_OUTPUT"

      - name: Get runner public IP
        id: whats-my-ip
        run: |
          IP=$(curl -s https://api.ipify.org)
          echo "ip=$IP" >> "$GITHUB_OUTPUT"
          echo "TF_VAR_github_ip=$IP" >> "$GITHUB_ENV"

      - name: Login with AZ
        uses: ./.github/actions/azure-login
        with:
          az_tenant_id: ${{ secrets.az_tenant_id }}
          az_subscription_id: ${{ secrets.az_subscription_id }}
          az_client_id: ${{ secrets.az_client_id }}

      - name: Whitelist Runner for TF keyvault
        uses: ./.github/actions/whitelist-kv
        with:
          ip-address: ${{ steps.whats-my-ip.outputs.ip }}
          resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}
          keyvault-name: ${{ steps.vars.outputs.TF_KEYVAULT_NAME }}
          verb: add

      - name: Whitelist Runner for TF storage
        uses: ./.github/actions/whitelist-storage
        with:
          ip-address: ${{ steps.whats-my-ip.outputs.ip }}
          resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}
          account-name: ${{ steps.vars.outputs.RESOURCE_PREFIX_TF_SHORT }}
          verb: add

      - name: Get secret and clean tfvars file
        shell: pwsh
        run: |
          ./.github/scripts/get-and-normalise-tfvars.ps1 `
            -SecretName "${{ secrets.TF_SECRET_NAME }}" `
            -KeyVaultName "${{ steps.vars.outputs.TF_KEYVAULT_NAME }}"

      - name: Whitelist Runner for app keyvault(in case exists)
        uses: ./.github/actions/whitelist-kv
        with:
          ip-address: ${{ steps.whats-my-ip.outputs.ip }}
          resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX }}
          keyvault-name: ${{ steps.vars.outputs.KEYVAULT_NAME }}
          verb: add

      - name: Build DNS URL
        shell: bash
        run: |
          case "$DEPLOY_ENVIRONMENT" in
            Dev)
              prefix="dev"
              ;;
            Test)
              prefix="test"
              ;;
            *Staging*)
              prefix="staging"
              ;;
            *Prod*)
              prefix="prod"
              ;;
            *)
              echo "Unsupported environment: $DEPLOY_ENVIRONMENT"
              exit 1
              ;;
          esac

          if [[ "${{ inputs.is_dr }}" == "true" ]]; then
            prefix="${prefix}-dr"
          fi

          echo "TF_VAR_dns_url=${prefix}${{ vars.BASE_DNS_URL }}" >> "$GITHUB_ENV"

      - name: Terraform init - app
        working-directory: ${{ env.TF_WORKING_DIRECTORY_APP }}
        run: |
          terraform init \
            -backend-config="resource_group_name=${{ steps.vars.outputs.RESOURCE_PREFIX_TF }}" \
            -backend-config="storage_account_name=${{ steps.vars.outputs.RESOURCE_PREFIX_TF_SHORT }}" \
            -backend-config="container_name=${{ env.TF_BACKEND_CONTAINER_NAME }}" \
            -backend-config="key=${{ env.TF_BACKEND_KEY }}"

      - name: Apply Terraform changes - rest of app
        working-directory: ${{ env.TF_WORKING_DIRECTORY_APP }}
        run: |
          terraform plan -input=false \
            -var-file="../../normalisedTerraformVars.tfvars" \
            -var="is_dr=${{ steps.vars.outputs.IS_DR }}" \
            -var="project_name=${{ steps.vars.outputs.PROJECT_PREFIX_APP }}" \
            -var="image_tag=${{ needs.image-and-acr.outputs.image_tag }}" \
            -var="workflow_runner_ip=${{ env.TF_VAR_github_ip }}" \
            -var="azure_location=northeurope" \
            -var="manage_dns_in_app_state=${ TF_VAR_dns_in_app }" \
            -var="primary_fqdn=${{ TF_VAR_dns_url }}"\
            -out=tfplanapp

            terraform show -no-color tfplanapp || true

            #terraform apply -auto-approve -input=false tfplanapp

  # restore-db:
  #   name: Restore DB from BACPAC
  #   needs: [apply-terraform]
  #   if: ${{ always() && !failure() && !cancelled() && (github.event_name == 'push' || inputs.restore-db) }}
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'Dev' }}
  #   permissions:
  #     id-token: write
  #     contents: read

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Prepare derived variables
  #       id: vars
  #       shell: bash
  #       run: |
  #         set -euo pipefail

  #         IS_DR="${{ github.event_name == 'push' && true || inputs.is_dr }}"

  #         if [ "$IS_DR" = "true" ]; then
  #           DFE_PROJECT_NAME="pt-dr"
  #         else
  #           DFE_PROJECT_NAME="${{ secrets.DFE_PROJECT_NAME }}"
  #         fi

  #         RESOURCE_PREFIX_BACKUP="${{ secrets.AZ_ENVIRONMENT }}${{ secrets.DFE_PROJECT_NAME }}-backup"  #not df
  #         RESOURCE_PREFIX_APP="${{ secrets.AZ_ENVIRONMENT }}${DFE_PROJECT_NAME}" #could be df

  #         echo "RESOURCE_PREFIX_BACKUP=$RESOURCE_PREFIX_BACKUP" >> "$GITHUB_OUTPUT"
  #         echo "RESOURCE_PREFIX_BACKUP_SHORT=${RESOURCE_PREFIX_BACKUP//-/}" >> "$GITHUB_OUTPUT"
  #         echo "RESOURCE_PREFIX_APP_SHORT=${RESOURCE_PREFIX_APP//-/}" >> "$GITHUB_OUTPUT"
  #         echo "RESOURCE_PREFIX_APP=$RESOURCE_PREFIX_APP" >> "$GITHUB_OUTPUT"

  #     - name: Login with AZ
  #       uses: ./.github/actions/azure-login
  #       with:
  #         az_tenant_id: ${{ secrets.az_tenant_id }}
  #         az_subscription_id: ${{ secrets.az_subscription_id }}
  #         az_client_id: ${{ secrets.az_client_id }}

  #     - name: Get runner public IP
  #       id: whats-my-ip
  #       run: |
  #         IP=$(curl -s https://api.ipify.org)
  #         echo "ip=$IP" >> "$GITHUB_OUTPUT"
  #         echo "TF_VAR_github_ip=$IP" >> "$GITHUB_ENV"

  #     - name: Restore DB from BACPAC
  #       uses: ./.github/actions/restore-db-from-bacpac
  #       with:
  #         resource_group_backup: ${{ steps.vars.outputs.RESOURCE_PREFIX_BACKUP }}
  #         resource_group_app: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         ip-address: ${{ steps.whats-my-ip.outputs.ip }}
  #         sql_server_name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         target_database: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}-sqldb
  #         storage_account: ${{ steps.vars.outputs.RESOURCE_PREFIX_BACKUP_SHORT }}
  #         container_name: ${{ env.BACKUP_CONTAINER_NAME }}
  #         bacpac_file: ${{ env.SQL_BACKUP_FILE_NAME }}
  #         bacpac_prefix: ${{ env.SQL_BACKUP_FILE_NAME_SEARCH }}
  #         use_latest_if_not_specified: ${{ env.USE_LATEST_IF_NOT_SPECIFIED }}

  #     - name: Whitelist Runner for sql
  #       uses: ./.github/actions/whitelist-sql
  #       with:
  #         ip-address: ${{ steps.whats-my-ip.outputs.ip }}
  #         resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         server-name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         verb: add

  #     - name: Grant Container App identity access to restored SQL DB
  #       shell: pwsh
  #       run: |
  #         $ErrorActionPreference = "Stop"

  #         Install-Module SqlServer -Force -Scope CurrentUser -AllowClobber

  #         $rg = "${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}"
  #         $sqlServer = "${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}"
  #         $sqlDb = "${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}-sqldb"

  #         $sqlFqdn = az sql server show `
  #           --resource-group $rg `
  #           --name $sqlServer `
  #           --query fullyQualifiedDomainName `
  #           -o tsv

  #         $identityResourceId = az containerapp identity show `
  #           --resource-group $rg `
  #           --name $rg `
  #           --query "userAssignedIdentities | keys(@)[0]" `
  #           -o tsv

  #         if ([string]::IsNullOrWhiteSpace($identityResourceId)) {
  #           throw "No user-assigned identity found on container app $rg"
  #         }

  #         $identityName = Split-Path $identityResourceId -Leaf

  #         $identityClientId = az identity show `
  #           --ids $identityResourceId `
  #           --query clientId `
  #           -o tsv

  #         if ([string]::IsNullOrWhiteSpace($identityClientId)) {
  #           throw "Could not resolve client/appId for identity $identityClientId"
  #         }

  #         $sidHex = python3 -c "import uuid; print('0x' + uuid.UUID('$identityClientId').bytes_le.hex())"

  #         $token = az account get-access-token `
  #           --resource https://database.windows.net/ `
  #           --query accessToken `
  #           -o tsv

  #         $query = @"
  #         IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = '$identityName')
  #         BEGIN
  #           CREATE USER [$identityName] WITH SID = $sidHex, TYPE = E;
  #         END;

  #         ALTER ROLE db_datareader ADD MEMBER [$identityName];
  #         ALTER ROLE db_datawriter ADD MEMBER [$identityName];
  #         ALTER ROLE db_ddladmin ADD MEMBER [$identityName];
  #         ALTER ROLE db_executor ADD MEMBER [$identityName];
  #         GRANT EXECUTE TO [$identityName];
  #         GRANT INSERT TO [$identityName];
  #         GRANT SELECT TO [$identityName];

  #         DELETE FROM dbo.DataProtectionKeys;

  #         "@

  #         Invoke-Sqlcmd `
  #           -ServerInstance $sqlFqdn `
  #           -Database $sqlDb `
  #           -AccessToken $token `
  #           -Query $query

  #     - name: De-Whitelist Runner for sql
  #       uses: ./.github/actions/whitelist-sql
  #       with:
  #         ip-address: ${{ steps.whats-my-ip.outputs.ip }}
  #         resource-group: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         server-name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         verb: remove

  # restore-tokens:
  #   name: Restore app tokens from backup kv
  #   #needs: [apply-terraform]
  #   if: ${{ always() && !failure() && !cancelled() && (github.event_name == 'push' || inputs.restore-tokens) }}
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'Dev' }}
  #   permissions:
  #     id-token: write
  #     contents: read

  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4

  #     - name: Azure CLI Login
  #       uses: ./.github/actions/azure-login
  #       with:
  #         az_tenant_id: ${{ secrets.AZ_TENANT_ID }}
  #         az_subscription_id: ${{ secrets.AZ_SUBSCRIPTION_ID }}
  #         az_client_id: ${{ secrets.AZ_CLIENT_ID }}

  #     - name: Prepare backup names
  #       id: backup_names
  #       shell: bash
  #       run: |
  #         BACKUP_RESOURCE_GROUP="${{ secrets.AZ_ENVIRONMENT }}${{ secrets.DFE_PROJECT_NAME }}-backup"
  #         BACKUP_KV="${{ secrets.AZ_ENVIRONMENT }}${{ secrets.DFE_PROJECT_NAME }}-backup"
  #         BACKUP_STORAGE_ACCOUNT="${BACKUP_RESOURCE_GROUP//-/}"

  #         echo "resource_group=$BACKUP_RESOURCE_GROUP" >> "$GITHUB_OUTPUT"
  #         echo "storage_account=$BACKUP_STORAGE_ACCOUNT" >> "$GITHUB_OUTPUT"
  #         echo "kv=$BACKUP_KV" >> "$GITHUB_OUTPUT"

  #     - name: Prepare app variables
  #       id: vars
  #       shell: bash
  #       run: |
  #         set -euo pipefail
  #         IS_DR="${{ github.event_name == 'push' && true || inputs.is_dr }}"

  #         if [ "$IS_DR" = "true" ]; then
  #           DFE_PROJECT_NAME="pt-dr"
  #         else
  #           DFE_PROJECT_NAME="${{ secrets.DFE_PROJECT_NAME }}"
  #         fi

  #         RESOURCE_PREFIX_APP="${{ secrets.AZ_ENVIRONMENT }}${DFE_PROJECT_NAME}"

  #         echo "RESOURCE_PREFIX_APP_SHORT=${RESOURCE_PREFIX_APP//-/}" >> "$GITHUB_OUTPUT"
  #         echo "RESOURCE_PREFIX_APP=$RESOURCE_PREFIX_APP" >> "$GITHUB_OUTPUT"

  #     - name: Restore app secrets
  #       uses: ./.github/actions/restore-app-secrets
  #       with:
  #         backup_resource_group: ${{ steps.backup_names.outputs.resource_group }}
  #         #note, no "-kv" as it was too long
  #         backup_keyvault: ${{ steps.backup_names.outputs.resource_group }}

  #         app_resource_group: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         app_keyvault: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}-kv

  #         backup_storage_account: ${{ steps.backup_names.outputs.storage_account }}
  #         secret_container_name: secretnames
  #         manifest_blob_name: secret-manifest.txt

  #         sql_server_secret_name: connectionstrings--database
  #         sql_server_python_secret_name: python--dbconnectionstring
  #         sql_server_name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}
  #         sql_database_name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}-sqldb

  #         redis_secret_name: connectionstrings--redis
  #         redis_name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}

  #         servicebus_secret_name: connectionstrings--servicebusconnection
  #         servicebus_namespace: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}servicebus

  #         appinsights_secret_name: applicationinsights--connectionstring
  #         appinsights_name: ${{ steps.vars.outputs.RESOURCE_PREFIX_APP }}-insights
